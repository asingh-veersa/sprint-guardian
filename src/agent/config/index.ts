/**
 * Threshold is set to identify all issues having more than 'LOW' severity.
 */
const RISK_THRESHOLD: number = 10;
const SPRINT_END_THRESHOLD: number = 2;

export { RISK_THRESHOLD, SPRINT_END_THRESHOLD };
